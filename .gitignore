# editors
.vscode
.idea

# ignore database copy for local dev work
*.gz

# local build/scratch output (binaries, logs, generated reports)
out/

# don't accidentally publish the binary
api

docker-compose.yaml

# Claude Code local working files
.claude/
docs/superpowers/
CLAUDE.md
.playwright-mcp/